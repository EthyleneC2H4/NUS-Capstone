% ── preamble.tex ──────────────────────────────────────────────────────────────
% All packages and global formatting settings.
% Included by main.tex via \input{preamble}.

% ── Page layout ───────────────────────────────────────────────────────────────
\usepackage[top=1in, bottom=1in, left=1in, right=1in]{geometry}

% ── Fonts & encoding ──────────────────────────────────────────────────────────
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{times}           % Times New Roman

% ── Spacing & paragraphs ──────────────────────────────────────────────────────
\usepackage{setspace}
\setstretch{1.15}
\usepackage{parskip}         % space between paragraphs, no indent

% ── Section heading styles ───────────────────────────────────────────────────
\usepackage{titlesec}
% Headings: 11pt, Bold, Left aligned
\titleformat{\section}
  {\normalfont\fontsize{11}{13}\bfseries}{\thesection}{1em}{}
% Subheadings: 10pt, Bold, Left aligned
\titleformat{\subsection}
  {\normalfont\fontsize{10}{12}\bfseries}{\thesubsection}{1em}{}
\titleformat{\subsubsection}
  {\normalfont\fontsize{10}{12}\bfseries}{\thesubsubsection}{1em}{}

% ── Tables & figures ──────────────────────────────────────────────────────────
\usepackage{booktabs}
\usepackage{array}
\usepackage{graphicx}
\graphicspath{{figures/}}    % all images live in figures/
\usepackage{caption}

% ── Math ──────────────────────────────────────────────────────────────────────
\usepackage{amsmath}
\usepackage{amssymb}

% ── URLs & hyperlinks ─────────────────────────────────────────────────────────
\usepackage{url}
\usepackage{hyperref}
\hypersetup{colorlinks=true, linkcolor=black, citecolor=black, urlcolor=blue}

% ── Page style (journal assigns page numbers) ────────────────────────────────
\pagestyle{empty}

% ── Footnote numbering ───────────────────────────────────────────────────────
\renewcommand{\thefootnote}{\arabic{footnote}}

% ── Manual citation shortcuts (reference list is a numbered enumerate) ────────
% Usage: \cite{emgnn2023} renders as [1], etc.
% We redefine \cite to map symbolic keys to reference list numbers.
\usepackage{etoolbox}
\let\origcite\cite
\renewcommand{\cite}[1]{%
  \ifstrequal{#1}{emgnn2023}{[1]}{%
  \ifstrequal{#1}{emogi2021}{[2]}{%
  \ifstrequal{#1}{gcn2017}{[3]}{%
  \ifstrequal{#1}{gat2018}{[4]}{%
  \ifstrequal{#1}{gin2019}{[5]}{%
  \ifstrequal{#1}{sage2017}{[6]}{%
  \ifstrequal{#1}{ig2017}{[7]}{%
  \ifstrequal{#1}{gsea2005}{[8]}{%
  \ifstrequal{#1}{enrichr2013}{[9]}{%
  \ifstrequal{#1}{msigdb2015}{[10]}{%
  \ifstrequal{#1}{hallmarks2011}{[11]}{%
  \ifstrequal{#1}{kingma2014adam}{[12]}{%
  \ifstrequal{#1}{batchnorm2015}{[13]}{%
  \ifstrequal{#1}{resnet2016}{[14]}{%
  \ifstrequal{#1}{cosine2017}{[15]}{%
  \ifstrequal{#1}{graphnorm2021}{[16]}{%
  \ifstrequal{#1}{cgc2022}{[17]}{%
  {[?]}}}}}}}}}}}}}}}}}}%
}
