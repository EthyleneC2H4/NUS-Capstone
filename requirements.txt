# Core deep learning
# Tested with PyTorch 2.2.2 (CPU). For GPU, replace with the CUDA variant:
#   pip install torch==2.2.2+cu118 -f https://download.pytorch.org/whl/torch_stable.html
torch==2.2.2
torch-geometric==2.7.0
# torch-scatter / torch-sparse / torch-cluster / torch-spline-conv are bundled
# in modern torch-geometric (>= 2.3) — install separately only for older versions.

# Explainability
captum==0.8.0

# Data & ML utilities
numpy==1.26.4
scipy==1.12.0
scikit-learn==1.7.2
pandas==2.2.2
h5py==3.11.0

# Visualisation
matplotlib==3.8.4
networkx==2.5.1

# Hyperparameter optimisation (optional)
optuna==4.8.0

# Gene set enrichment analysis (optional)
gseapy==1.1.13

# Notebook
jupyter==1.0.0
